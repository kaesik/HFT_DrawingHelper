<Window x:Class="HFT_DrawingHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="HFT Drawing Helper"
        MinWidth="420"
        SizeToContent="WidthAndHeight"
        MaxHeight="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="Segoe UI Variable, Segoe UI">

    <Window.Resources>
        <Style x:Key="SettingsComboBoxItemStyle"
               TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground"
                    Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch" />
            <Setter Property="Padding"
                    Value="10,6" />
            <Setter Property="FontSize"
                    Value="12" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="itemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3"
                                Margin="3,1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted"
                                     Value="True">
                                <Setter TargetName="itemBorder"
                                        Property="Background"
                                        Value="{DynamicResource SurfaceHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter TargetName="itemBorder"
                                        Property="Background"
                                        Value="{DynamicResource AccentColorBrush}" />
                                <Setter Property="Foreground"
                                        Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsComboBoxStyle"
               TargetType="{x:Type ComboBox}">
            <Setter Property="Background"
                    Value="{DynamicResource ControlBgBrush}" />
            <Setter Property="Foreground"
                    Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush"
                    Value="{DynamicResource DividerColorBrush}" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Padding"
                    Value="6,4" />
            <Setter Property="MaxDropDownHeight"
                    Value="200" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="ItemContainerStyle"
                    Value="{StaticResource SettingsComboBoxItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <!-- Zamknięty kontrolka -->
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="28" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="ContentSite"
                                                      Grid.Column="0"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="8,0,0,0"
                                                      VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="&#xE70D;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="10"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               IsHitTestVisible="False" />
                                    <ToggleButton Grid.Column="0"
                                                  Grid.ColumnSpan="2"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <!-- Dropdown popup -->
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Background="{DynamicResource SurfaceBgBrush}"
                                        BorderBrush="{DynamicResource BorderColorBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        SnapsToDevicePixels="True">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8"
                                                          ShadowDepth="2"
                                                          Opacity="0.4"
                                                          Color="#000000" />
                                    </Border.Effect>
                                    <ScrollViewer Background="Transparent"
                                                  Margin="2">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ControlHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsDropDownOpen"
                                     Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource AccentColorBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource WindowBgBrush}"
            BorderBrush="{DynamicResource WindowBorderBrushBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12"
                              ShadowDepth="2"
                              Opacity="0.4"
                              Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Grid.ColumnSpan="3"
                    Background="{DynamicResource TitleBarBgBrush}"
                    CornerRadius="7,7,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Rectangle Grid.Column="1"
                               Width="32"
                               Height="32"
                               Fill="{DynamicResource TextPrimaryBrush}"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               VerticalAlignment="Center">
                        <Rectangle.OpacityMask>
                            <ImageBrush ImageSource="pack://application:,,,/Assets/logo.png"
                                        Stretch="Uniform" />
                        </Rectangle.OpacityMask>
                    </Rectangle>

                    <TextBlock Grid.Column="2"
                               Text="DRAWING HELPER"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0" />

                    <StackPanel Grid.Column="3"
                                Orientation="Horizontal"
                                VerticalAlignment="Stretch">
                        <Button x:Name="SettingsToggleButton"
                                Style="{StaticResource IconButton}"
                                Content="&#xE713;"
                                ToolTip="Ustawienia przekrojów"
                                Click="SettingsToggleButton_Click" />
                        <Button x:Name="ThemeToggleButton"
                                Style="{StaticResource IconButton}"
                                Content="&#xE793;"
                                ToolTip="Toggle theme"
                                Click="ThemeToggle_Click" />
                        <Button x:Name="CloseButton"
                                Style="{StaticResource CloseButton}"
                                Content="&#xE8BB;"
                                ToolTip="Close"
                                Click="CloseButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border x:Name="SettingsPanelBorder"
                    Grid.Row="1"
                    Grid.Column="0"
                    Visibility="Collapsed"
                    BorderBrush="{DynamicResource DividerColorBrush}"
                    BorderThickness="0,0,1,0">

                <Grid Width="260"
                      Margin="14,10,14,14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0"
                          Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="USTAWIENIA PRZEKROJÓW"
                                   Foreground="{DynamicResource TextTertiaryBrush}"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />

                        <Button Grid.Column="1"
                                Style="{StaticResource IconButton}"
                                Content="&#xE8BB;"
                                Width="22"
                                Height="22"
                                FontSize="10"
                                ToolTip="Zamknij panel"
                                Click="CloseSettingsPanelButton_Click" />
                    </Grid>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="ViewAttributeName"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   FontSize="11"
                                   Margin="0,0,0,4" />

                        <ComboBox x:Name="ViewAttributeNameComboBox"
                                  Style="{StaticResource SettingsComboBoxStyle}"
                                  Height="32"
                                  Margin="0,0,0,10"
                                  FontSize="12"
                                  IsEditable="False"
                                  IsTextSearchEnabled="True" />

                        <TextBlock Text="MarkAttributeName"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   FontSize="11"
                                   Margin="0,0,0,4" />

                        <ComboBox x:Name="MarkAttributeNameComboBox"
                                  Style="{StaticResource SettingsComboBoxStyle}"
                                  Height="32"
                                  Margin="0,0,0,12"
                                  FontSize="12"
                                  IsEditable="False"
                                  IsTextSearchEnabled="True" />

                        <Button x:Name="RefreshSectionSettingsButton"
                                Content="Odśwież listę"
                                Style="{StaticResource SecondaryButton}"
                                HorizontalAlignment="Stretch"
                                Height="32"
                                Margin="0,0,0,8"
                                Click="RefreshSectionSettingsButton_Click" />

                        <Button x:Name="ApplySectionSettingsButton"
                                Content="Zastosuj"
                                Style="{StaticResource PrimaryButton}"
                                HorizontalAlignment="Stretch"
                                Height="32"
                                Click="ApplySectionSettingsButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1"
                        Grid.Column="1"
                        Margin="14,10,14,14">

                <TextBlock Text="KRAWĘDZIE"
                           Foreground="{DynamicResource TextTertiaryBrush}"
                           FontSize="10"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="DrawEdgesButton"
                            Grid.Column="0"
                            Content="Rysuj krawędzie"
                            Style="{StaticResource PrimaryButton}"
                            HorizontalAlignment="Stretch"
                            Height="32"
                            Click="DrawEdgesButton_Click" />

                    <Button x:Name="GetEdgesButton"
                            Grid.Column="2"
                            Content="Zdobądź krawędzie"
                            Style="{StaticResource SecondaryButton}"
                            HorizontalAlignment="Stretch"
                            Height="32"
                            Click="GetEdgesButton_Click" />
                </Grid>

                <Border Height="1"
                        Background="{DynamicResource DividerColorBrush}"
                        Margin="0,14,0,14" />

                <TextBlock Text="PRZEKROJE"
                           Foreground="{DynamicResource TextTertiaryBrush}"
                           FontSize="10"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6" />

                <Button x:Name="AddSectionsButton"
                        Content="Dodaj przekroje"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Stretch"
                        Height="32"
                        Click="AddSectionsButton_Click" />

                <Border Height="1"
                        Background="{DynamicResource DividerColorBrush}"
                        Margin="0,14,0,14" />

                <TextBlock Text="WYMIAROWANIE"
                           Foreground="{DynamicResource TextTertiaryBrush}"
                           FontSize="10"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6" />

                <Button x:Name="ShowSelectedPartsButton"
                        Content="Pokaż zaznaczone"
                        Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Stretch"
                        Height="32"
                        Margin="0,0,0,10"
                        Click="ShowSelectedPartsButton_Click" />

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Typ"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="11"
                               VerticalAlignment="Center" />

                    <Grid Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <RadioButton Grid.Column="0"
                                     Content="Prostoliniowy"
                                     Style="{StaticResource HftRadioButton}"
                                     GroupName="DimType"
                                     IsChecked="True" />

                        <RadioButton x:Name="AngledDimensionRadioButton"
                                     Grid.Column="1"
                                     Content="Pod kątem"
                                     Style="{StaticResource HftRadioButton}"
                                     GroupName="DimType" />

                        <RadioButton x:Name="CurvedDimensionRadioButton"
                                     Grid.Column="2"
                                     Content="Łukowy"
                                     Style="{StaticResource HftRadioButton}"
                                     GroupName="DimType" />
                    </Grid>
                </Grid>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Poziom"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="11"
                               VerticalAlignment="Center" />

                    <CheckBox x:Name="DimensionAboveCheckBox"
                              Grid.Column="1"
                              Content="Powyżej"
                              Style="{StaticResource HftCheckBox}"
                              IsChecked="True"
                              VerticalAlignment="Center" />

                    <CheckBox x:Name="DimensionBelowCheckBox"
                              Grid.Column="2"
                              Content="Poniżej"
                              Style="{StaticResource HftCheckBox}"
                              VerticalAlignment="Center" />

                    <CheckBox x:Name="HorizontalTotalDimensionCheckBox"
                              Grid.Column="3"
                              Content="Całkowity"
                              Style="{StaticResource HftCheckBox}"
                              IsChecked="True"
                              VerticalAlignment="Center" />
                </Grid>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Pion"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="11"
                               VerticalAlignment="Center" />

                    <CheckBox x:Name="DimensionRightCheckBox"
                              Grid.Column="1"
                              Content="Prawa"
                              Style="{StaticResource HftCheckBox}"
                              IsChecked="True"
                              VerticalAlignment="Center" />

                    <CheckBox x:Name="DimensionLeftCheckBox"
                              Grid.Column="2"
                              Content="Lewa"
                              Style="{StaticResource HftCheckBox}"
                              VerticalAlignment="Center" />

                    <CheckBox x:Name="VerticalTotalDimensionCheckBox"
                              Grid.Column="3"
                              Content="Całkowity"
                              Style="{StaticResource HftCheckBox}"
                              IsChecked="True"
                              VerticalAlignment="Center" />
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Linia"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="11"
                               VerticalAlignment="Center" />

                    <CheckBox x:Name="ShortExtensionLineCheckBox"
                              Grid.Column="1"
                              Content="Krótka"
                              Style="{StaticResource HftCheckBox}"
                              VerticalAlignment="Center"
                              ToolTip="Ukrywa linie pomocnicze wymiaru." />
                </Grid>

                <Button x:Name="AddDimensionsButton"
                        Content="Dodaj wymiary"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Stretch"
                        Height="32"
                        Click="AddDimensionsButton_Click" />

                <Border Height="1"
                        Background="{DynamicResource DividerColorBrush}"
                        Margin="0,14,0,10" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Model:"
                               Foreground="{DynamicResource TextTertiaryBrush}"
                               FontSize="10"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0" />

                    <TextBlock x:Name="ModelDrawingLabel"
                               Grid.Column="1"
                               Foreground="{DynamicResource ValueHighlightBrush}"
                               FontSize="11"
                               FontWeight="Bold"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                </Grid>
            </StackPanel>

            <Border x:Name="SidePanelBorder"
                    Grid.Row="1"
                    Grid.Column="2"
                    Visibility="Collapsed"
                    BorderBrush="{DynamicResource DividerColorBrush}"
                    BorderThickness="1,0,0,0">

                <Grid Width="260"
                      Margin="14,10,14,14">

                    <Grid x:Name="PartSelectionPanel"
                          Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0"
                              Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="ZAZNACZONE ELEMENTY"
                                       Foreground="{DynamicResource TextTertiaryBrush}"
                                       FontSize="10"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center" />

                            <Button Grid.Column="1"
                                    Style="{StaticResource IconButton}"
                                    Content="&#xE8BB;"
                                    Width="22"
                                    Height="22"
                                    FontSize="10"
                                    ToolTip="Zamknij panel"
                                    Click="CloseSidePanelButton_Click" />
                        </Grid>

                        <TextBlock Grid.Row="1"
                                   Text="Zaznacz elementy do zwymiarowania:"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,8" />

                        <StackPanel Grid.Row="2"
                                    Orientation="Horizontal"
                                    Margin="0,0,0,8">
                            <Button Content="Zaznacz wszystkie"
                                    Style="{StaticResource SecondaryButton}"
                                    Height="26"
                                    FontSize="11"
                                    Margin="0,0,4,0"
                                    Click="SelectAllPartsButton_Click" />
                            <Button Content="Odznacz wszystkie"
                                    Style="{StaticResource SecondaryButton}"
                                    Height="26"
                                    FontSize="11"
                                    Click="DeselectAllPartsButton_Click" />
                        </StackPanel>

                        <ListBox x:Name="PartItemsList"
                                 Grid.Row="3"
                                 MaxHeight="400"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.CanContentScroll="False">

                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,0,0,6" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Focusable" Value="False" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource ControlBgBrush}"
                                            BorderBrush="{DynamicResource DividerColorBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="0">

                                        <StackPanel>
                                            <Border CornerRadius="4"
                                                    Padding="0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Transparent" />
                                                        <Setter Property="BorderBrush" Value="Transparent" />
                                                        <Setter Property="BorderThickness" Value="1.5" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsPreviewSelected}"
                                                                         Value="True">
                                                                <Setter Property="Background"
                                                                        Value="{DynamicResource ControlBgBrush}" />
                                                                <Setter Property="BorderBrush"
                                                                        Value="{DynamicResource AccentColorBrush}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                                <Grid Margin="8,6"
                                                      MouseLeftButtonDown="GroupHeader_MouseLeftButtonDown">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>

                                                    <CheckBox Grid.Column="0"
                                                              IsThreeState="True"
                                                              IsChecked="{Binding IsChecked, Mode=OneWay}"
                                                              Click="GroupCheckBox_Click"
                                                              Style="{StaticResource HftCheckBox}"
                                                              VerticalAlignment="Center" />

                                                    <TextBlock Grid.Column="1"
                                                               Text="{Binding GroupName}"
                                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                                               FontSize="12"
                                                               FontWeight="SemiBold"
                                                               VerticalAlignment="Center"
                                                               Margin="8,0,0,0" />

                                                    <StackPanel Grid.Column="2"
                                                                Orientation="Horizontal"
                                                                VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding ItemCount, StringFormat=({0})}"
                                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                                   FontSize="12"
                                                                   Margin="0,0,8,0"
                                                                   VerticalAlignment="Center" />

                                                        <ToggleButton IsChecked="{Binding IsExpanded, Mode=TwoWay}"
                                                                      Width="22"
                                                                      Height="22"
                                                                      Background="Transparent"
                                                                      BorderThickness="0"
                                                                      Cursor="Hand">
                                                            <TextBlock Text="&#xE70D;"
                                                                       FontFamily="Segoe MDL2 Assets"
                                                                       FontSize="12"
                                                                       HorizontalAlignment="Center"
                                                                       VerticalAlignment="Center">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="RenderTransformOrigin"
                                                                            Value="0.5,0.5" />
                                                                        <Setter Property="RenderTransform">
                                                                            <Setter.Value>
                                                                                <RotateTransform Angle="0" />
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Setter Property="Foreground"
                                                                            Value="{DynamicResource TextSecondaryBrush}" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsExpanded}"
                                                                                Value="True">
                                                                                <Setter Property="RenderTransform">
                                                                                    <Setter.Value>
                                                                                        <RotateTransform Angle="180" />
                                                                                    </Setter.Value>
                                                                                </Setter>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </ToggleButton>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>

                                            <ItemsControl ItemsSource="{Binding Items}">
                                                <ItemsControl.Style>
                                                    <Style TargetType="ItemsControl">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsExpanded}"
                                                                         Value="True">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ItemsControl.Style>

                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Padding="6,5"
                                                                CornerRadius="4"
                                                                Margin="14,1,8,1"
                                                                MouseLeftButtonDown="PartItem_MouseLeftButtonDown">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="Transparent" />
                                                                    <Setter Property="BorderBrush" Value="Transparent" />
                                                                    <Setter Property="BorderThickness" Value="1.5" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger
                                                                            Binding="{Binding IsPreviewSelected}"
                                                                            Value="True">
                                                                            <Setter Property="Background"
                                                                                Value="{DynamicResource ControlBgBrush}" />
                                                                            <Setter Property="BorderBrush"
                                                                                Value="{DynamicResource AccentColorBrush}" />
                                                                        </DataTrigger>
                                                                        <Trigger Property="IsMouseOver"
                                                                            Value="True">
                                                                            <Setter Property="Background"
                                                                                Value="{DynamicResource SurfaceHoverBrush}" />
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>

                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="*" />
                                                                </Grid.ColumnDefinitions>

                                                                <CheckBox Grid.Column="0"
                                                                          IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                                          Style="{StaticResource HftCheckBox}"
                                                                          VerticalAlignment="Center" />

                                                                <StackPanel Grid.Column="1"
                                                                            Margin="8,0,0,0">
                                                                    <TextBlock Text="{Binding DisplayName}"
                                                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                                                               FontSize="12"
                                                                               TextTrimming="CharacterEllipsis" />

                                                                    <TextBlock Text="{Binding SecondaryInfo}"
                                                                               Foreground="{DynamicResource TextTertiaryBrush}"
                                                                               FontSize="10"
                                                                               TextTrimming="CharacterEllipsis"
                                                                               Margin="0,1,0,0">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Style.Triggers>
                                                                                    <Trigger Property="Text"
                                                                                        Value="">
                                                                                        <Setter Property="Visibility"
                                                                                            Value="Collapsed" />
                                                                                    </Trigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </StackPanel>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>

                    <Grid x:Name="EdgeSelectionPanel"
                          Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0"
                              Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="KRAWĘDZIE"
                                       Foreground="{DynamicResource TextTertiaryBrush}"
                                       FontSize="10"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center" />

                            <Button Grid.Column="1"
                                    Style="{StaticResource IconButton}"
                                    Content="&#xE8BB;"
                                    Width="22"
                                    Height="22"
                                    FontSize="10"
                                    ToolTip="Zamknij panel"
                                    Click="CloseSidePanelButton_Click" />
                        </Grid>

                        <TextBlock Grid.Row="1"
                                   Text="Kliknij wiersz, aby pokazać podgląd. Checkbox oznacza krawędź do przekroju."
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,8" />

                        <StackPanel Grid.Row="2"
                                    Orientation="Horizontal"
                                    Margin="0,0,0,8">
                            <Button Content="Zaznacz wszystkie"
                                    Style="{StaticResource SecondaryButton}"
                                    Height="26"
                                    FontSize="11"
                                    Margin="0,0,4,0"
                                    Click="SelectAllEdgesButton_Click" />
                            <Button Content="Odznacz wszystkie"
                                    Style="{StaticResource SecondaryButton}"
                                    Height="26"
                                    FontSize="11"
                                    Click="DeselectAllEdgesButton_Click" />
                        </StackPanel>

                        <ListBox x:Name="EdgeGroupsList"
                                 Grid.Row="3"
                                 MaxHeight="400"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,0,0,6" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Focusable" Value="False" />
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="8,6"
                                            CornerRadius="4"
                                            Margin="0"
                                            MouseLeftButtonDown="EdgeGroupItem_MouseLeftButtonDown">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background"
                                                        Value="{DynamicResource ControlBgBrush}" />
                                                <Setter Property="BorderBrush"
                                                        Value="{DynamicResource DividerColorBrush}" />
                                                <Setter Property="BorderThickness"
                                                        Value="1" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPreviewVisible}" Value="True">
                                                        <Setter Property="Background"
                                                                Value="{DynamicResource ControlBgBrush}" />
                                                        <Setter Property="BorderBrush"
                                                                Value="{DynamicResource AccentColorBrush}" />
                                                        <Setter Property="BorderThickness"
                                                                Value="1.5" />
                                                    </DataTrigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background"
                                                                Value="{DynamicResource SurfaceHoverBrush}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <CheckBox Grid.Column="0"
                                                      IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                      Style="{StaticResource HftCheckBox}"
                                                      VerticalAlignment="Top"
                                                      PreviewMouseLeftButtonDown="EdgeGroupCheckBox_PreviewMouseLeftButtonDown" />

                                            <StackPanel Grid.Column="1"
                                                        Margin="8,0,0,0">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           FontSize="12"
                                                           FontWeight="SemiBold" />

                                                <TextBlock Text="{Binding SecondaryInfo}"
                                                           Foreground="{DynamicResource TextTertiaryBrush}"
                                                           FontSize="10"
                                                           TextWrapping="Wrap"
                                                           Margin="0,2,0,0" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>